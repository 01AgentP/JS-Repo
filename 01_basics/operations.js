let value = 3
let negValue = -value

console.log(negValue);


// Pre and Post increment in JS

let num = 10;

console.log(num++); // Prints 10 (old value), then num becomes 11
console.log(num);   // Now num is 11

console.log(++num); // First, num becomes 12, then prints 12
console.log(num);   // num is still 12

